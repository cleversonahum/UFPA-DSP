[B, A] = butter(3,1,'s')
[Bnew, Anew] = lp2lp(B,A,1200*pi)
s_values = [0, 1i, 1200*pi*i, 2000*i]; 

% Letra a)
dis
for s=s_values
  Hs(A,B,s);
end

function H_s = Hs(a,b,s)
  disp(["\nPara s = ", num2str(s), " :\n"])
  s_mtx = [z^3, z^2, z^1, z^0]';
  H_s = (b*s_mtx)/(a*s_mtx)
end